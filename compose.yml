#version: '3.9'
services:
  app:
    image: "starcitizen_trading_v1"
    container_name: 'starcitizen_trading'
    restart: always
    network_mode: host
    build:
      context: .
      dockerfile: Dockerfile